/**
 * Email message collection class
 */
var EmailattachmentCollection = Backbone.Collection.extend({
    /**
     * The model class to use in the collection
     */
    model: Emailattachment
});